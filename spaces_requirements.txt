# Minimal deps for the HF Space (rename to requirements.txt in the Space repo).
# Deliberately excludes the training stack (tensorflow, datasets, peft, ...) — the Space
# only does inference, and a lean build = faster cold starts.
torch>=2.1,<3.0
transformers>=4.40,<5.0
streamlit>=1.32
anthropic>=0.25,<1.0
python-dotenv>=1.0
