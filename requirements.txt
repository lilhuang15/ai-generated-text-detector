# Deployment requirements — only what the live demo (app.py) imports, fully pinned.
# Streamlit Community Cloud reinstalls this file on every container rebuild; a small,
# pinned list keeps rebuilds fast and deterministic so the public demo stays up.
# Versions match the research env (conda "aidetect") — the demo runs the same stack
# that produced the reported numbers.
#
# Full research stack (notebooks 01-04, training): requirements-dev.txt

# CPU-only torch wheels — Streamlit Cloud has no GPU; skips the multi-GB CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.58.0
torch==2.12.0
transformers==4.57.6
huggingface_hub==0.36.2
anthropic==0.105.2
python-dotenv==1.2.2
